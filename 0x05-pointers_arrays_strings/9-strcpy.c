#include "main.h"

/**
* _strcpy - copies the string src to the string dest
* @dest: string to be copied into
* @src: string the string to be copied
* Return: dest
*/
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
